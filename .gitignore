# Sensitive configuration files
.env
.env.local
.env.production

# Composer vendor directory
vendor/

# CodeIgniter writable directories (runtime data)
writable/logs/
writable/cache/
writable/session/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Docker override files with secrets
docker-compose.override.yml

# Old legacy config (no longer exists, kept for safety)
config/config.php
